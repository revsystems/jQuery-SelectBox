(function($) {
  
  var SelectBox = Class.extend({
    
  });
  
  function Display() {
  }
  
  function Dropdown() {
  }
  
  function Option() {
  }
  
  $.fn.sb = function(options) {
    
  };
}(jQuery));